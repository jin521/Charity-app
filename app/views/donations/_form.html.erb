
<%= form_for(@donation) do |f| %>
  <% if @donation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donation.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @donation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :campaign_id %><br>
    <%= f.text_field :campaign_id, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :amount %><br>
    <%= f.text_field :amount, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :credit_card_details %><br>
    <%= f.text_field :credit_card_details, class: "form-control" %>
  </div>
  <br>
  <div class="form-group">
    <%= f.submit "Donate", class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
