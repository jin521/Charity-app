<%= form_with(model: donation, local: true) do |form| %>
  <% if donation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donation.errors.count, "error") %> prohibited this donation from being saved:</h2>

      <ul>
      <% donation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :campaign_id %>
    <%= form.number_field :campaign_id, id: :donation_campaign_id %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_area :email, id: :donation_email %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :donation_amount %>
  </div>

  <div class="field">
    <%= form.label :credit_card_details %>
    <%= form.text_area :credit_card_details, id: :donation_credit_card_details %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :donation_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
